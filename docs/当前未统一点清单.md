# 当前未统一点清单

## 已完成的统一项
- 页面壳已统一
- 状态呈现区已组件化
- 备注区已组件化
- 摘要统计块已组件化
- 分区块与表格容器已开始组件化
- Dashboard 保留了演示项目入口

## 当前仍残留的不统一点

### 1. 输入页内部仍有部分旧样式
- `src/pages/Owner/OwnerInterview.module.css`
- `src/pages/Vendor/VendorCapability.module.css`
- `src/pages/Integrator/IntegratorWorkspace.module.css`

主要表现：
- 仍有少量局部区块使用页面私有边框和旧高亮样式
- 个别交互块仍是页面内定制样式，而不是通用容器

### 2. 结果页内部摘要区尚未完全共享组件
- `src/pages/Owner/OwnerResult.jsx`
- `src/pages/Integrator/IntegratorResult.jsx`

主要表现：
- 摘要信息布局已趋同，但仍有局部结果块未抽到统一组件

### 3. 闭环页内部状态标签仍是页面私有实现
- `src/pages/Selection/SelectionMatrix.module.css`

主要表现：
- 严重度颜色与状态 badge 仍由页面自己控制
- 后续适合抽为统一 `StatusBadge` 组件

### 4. Landing / Learning / Gap 等辅助页尚未完全纳入同一等级的组件化收口
- `src/pages/Landing/*`
- `src/pages/Learning/*`
- `src/pages/Gap/*`

主要表现：
- 这些页面的整体方向已基本协调，但尚未像主流程页一样深度收敛

## 下一步推荐顺序
1. 抽 `StatusBadge` 组件
2. 抽输入页的统一 `StepTabs` 组件
3. 清理 `OwnerInterview` / `VendorCapability` / `IntegratorWorkspace` 残留旧样式
4. 最后统一 Landing / Learning / Gap 辅助页

